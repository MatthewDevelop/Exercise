<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foxconn.matthew.boardcasttest">

    <permission
        android:name="com.foxconn.matthew.broadcasttest.MY_PERMISSION"
        android:protectionLevel="normal" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.foxconn.matthew.broadcasttest.MY_PERMISSION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.foxconn.matthew.bestbroadcasttest.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <receiver
            android:name=".BootCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".MyBroadCastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.foxconn.matthew.broadcasttest.MY_PERMISSION">
            <intent-filter android:priority="100">
                <action android:name="com.foxconn.matthew.broadcasttest.MY_BROADCAST" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".AnotherBroadCastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.foxconn.matthew.broadcasttest.MY_PERMISSION">
            <intent-filter android:priority="50">
                <action android:name="com.foxconn.matthew.broadcasttest.MY_BROADCAST" />
            </intent-filter>
        </receiver>
    </application>

</manifest>